function displayEvent(eventHTML) {
  const editPage = document.querySelector('.edit-page');
  editPage.insertAdjacentHTML('beforeend', eventHTML);
}

displayEvent('<%= j render "show_event", event: @event %>');
